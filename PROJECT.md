# バレーボール試合記録アプリ

## 現在の範囲

- 試合開始、記録中試合の再開
- セット別の点数・スタッツ入力と操作順Undo
- セット終了、次セット開始、試合終了
- 過去試合一覧、試合詳細、セット別／試合合計スタッツ
- Android、iOS、Webで共通の端末内自動保存

日別集計、スコアとスタッツの自動連動、クラウド同期、ログインは未実装。公開中のWeb版とAPKには今回の変更をまだ反映していない。

## 旧PCへの引継ぎ（2026-09-14）

- 作業元は `main`、開始時HEADは `f7e3c39005868dae1feddcd644d154e4c7545ce4`。ブロック3ボタン、レセプションA/B/ミス、保存v4と旧v1/v2/v3移行、入力順、直前の入力ラベル、600ms強調をまとめて引き継ぐ。
- 型検査・ESLint成功。代替実行で7ファイル53テスト成功（block 13、input-highlight 5、match-domain 7、match-storage 6、reception 9、session-storage 7、volleyball-stats 6）。標準テストの子プロセス起動はCodex環境で `spawn EPERM` だったため、代替成功と区別する。
- 利用者報告：通常のVS CodeターミナルでWeb export成功。ローカルWebでブロック等の表示変更を確認済み。Codex環境のexportはMetro子プロセス起動の `spawn EPERM` で失敗したが、通常ターミナルでの成功を確認した。
- リモート操作不調のため、実画面でのタップ・Undo・再読み込み後の保存復元は未確認。自動テストのメモリ保存検証と、ブラウザでの操作確認を区別する。
- 公開中のWeb版とAPKは未更新。次は旧PCで操作確認を行い、その後に公開版更新を検討する。今回のpushはソース引継ぎであり、公開操作や完成承認ではない。
- 実装の所在：`components/stats-panel.tsx` と `app/index.tsx` が入力・表示・解除連携、`lib/input-highlight.ts` が600ms強調、`lib/volleyball-stats.ts` が新旧集計、`lib/match-domain.ts`・`lib/match-storage.ts`・`lib/session-storage.ts` が保存v4と移行。関連検証は `tests/`、仕様は本書。
- `node_modules`、`dist`、PADテストファイル、リザルト、秘密情報はコミット対象外。端末の実データは移さず、独立したテスト用保存領域を使う。

旧PCではローカル差分を確認してからmainの更新を取り込み、必要なら既存lockfileに従い `npm.cmd ci`。確認コマンドは以下。

```powershell
npx.cmd tsc --noEmit
npm.cmd run lint
npm.cmd test
# 標準テストがspawn EPERMの場合だけ、同じ対象を代替実行
node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --experimental-strip-types --experimental-test-isolation=none --test tests/*.test.ts
npx.cmd expo export --platform web --output-dir dist
```

既存のdistに手作業ファイルがあれば別出力先を使う。空きポートを確認し、生成物だけをlocalhostへ配信する。テスト用ブラウザプロファイルで、ブロック「飛んだ2・ワンタッチ1・成功1」→総4・両率25.0%、成功Undo→総3・0.0%/33.3%、レセプション「A2・B1・ミス1」→総4・50.0%/25.0%/75.0%、ミスUndo→総3・66.7%/33.3%/100.0%を確認する。得点不変、連打全件、600ms強調と解除、スクロール無加算、保存済み後の同一URL再読み込み・復元後Undoも確認する。公開版とは別オリジンのため公開版の記録は自動で現れない。過去記録消失は原因不明・聞き取り待ちのまま。

## 試合開始

- 日付は端末の現地日付（YYYY-MM-DD）を初期値にする
- 自チーム名の初期値は「自チーム」。変更値は次回へ引き継ぐ
- 相手チーム名は必須
- 自チームは常に左、相手チームは常に右
- 開始時に第1セットを作成する
- 記録中試合がある間は新規試合を開始せず、「記録を再開」する

## 試合中・セット

- 上部に現在セット番号、両チーム名、手動点数を表示する
- 点数は「＋1」または直接入力で変更し、0以上の整数だけを確定する
- 空欄・不正入力は自動的に0へ変換しない
- 点数未確定でもスタッツ入力できる
- スタッツ操作から点数は自動加算しない
- 14個のスタッツボタン、全内訳、暫定率、直前操作を維持する
- Undoは現在セット内のスタッツ、＋1、点数修正を操作順に戻す
- セット確定後は通常のUndoで境界をまたがない

セット終了時は点数を明示的に確認する。同点または未確定は警告後の明示操作でそのまま保存できる。セット数上限、25点、2点差、自動試合終了は設けない。

## 結果表示・修正

- セットを上から順に縦表示する
- 表示順：自チームマーク、自チーム点数、セット番号、相手点数、相手マーク
- 勝ち「● 勝」、負け「○ 負」。同点、未確定、記録中はマークを付けず文字表示
- 勝敗は確定点数から計算し、点数修正後に再計算する
- 終了セットの点数修正はスタッツへ影響しない
- 試合合計スタッツは全イベントを合算し、合計本数と内訳から率を再計算する（セット率の単純平均ではない）

## 暫定の率計算

カウントは整数で保持し、表示時だけ小数1桁へ丸める。分母0は0.0%。監督確認前の暫定式。

- サーブ決定率 = エース数 ÷ サーブ本数 × 100
- サーブミス率 = ミス数 ÷ サーブ本数 × 100
- 新レセプションA率 = A ÷ 新本数 × 100
- 新レセプションB率 = B ÷ 新本数 × 100
- 新レセプション成功率 = (A+B) ÷ 新本数 × 100
- 旧レセプション成功率 = 旧成功 ÷ 旧本数 × 100（旧記録欄）
- 新ブロック成功率 = 新成功数 ÷ 総ジャンプ数 × 100
- 新ブロックワンタッチ率 = ワンタッチ数 ÷ 総ジャンプ数 × 100
- 旧ブロック成功率 = 旧成功数 ÷ 旧本数 × 100（旧記録欄のみ）
- ディグ成功率 = 成功数 ÷ 本数 × 100
- スパイク決定率 = 成功数 ÷ 本数 × 100

スパイクの「成功」「通常」「ミス」の厳密な判定基準と、各率の正式定義は確認待ち。得失点には連動させない。

## 保存仕様 version 4

### Web版（Safari）の保存条件

- 記録は端末・ブラウザごとに別データとして、開いている端末のSafari内へ保存される。
- プライベートブラウズでの使用は推奨しない。
- Safariの履歴だけでなく、Webサイトデータを削除すると記録が消える可能性がある。
- 同じURLを別端末で開いても記録は引き継がれない。
- クラウド保存・バックアップは未実装。
- 初回テストでは紙の記録も併用する。
- 画面上部が「保存中」の間は最新操作の保存が完了していないため、「保存済み」の表示を確認してからタブを閉じる。

- AsyncStorageキー：`volleyball-app.match-database.v4`
- 固有ID、日付、両チーム名、試合状態、全セット、点数、操作履歴を保存
- 集計値と率は保存せず、セットのイベント履歴から再計算
- 通常操作はメモリへ即時反映し、直列書込キューで順番に保存
- 読込完了前は操作を無効化し、空データで上書きしない
- 読込失敗、破損、未対応versionは自動削除・上書きしない
- 書込失敗時は未保存表示と再試行を出し、メモリ上の記録を保持する
- 試合終了は保存成功後だけ完了画面へ進み、同じ試合IDを履歴に重複追加しない

### version 1 / 2 / 3からの移行

1. version 4があればそれだけを読み、破損・未対応version・読込失敗時に旧キーで上書きしない。
2. version 4がない場合はversion 3、次にversion 2の順で読む。全試合・セット・日付・チーム名・点数・操作履歴を保持しversion 4へ保存する。旧レセプションsuccess/missも旧ブロックsuccess/failureも値を変えない。
3. version 4 / 3 / 2がなくversion 1がある場合、旧履歴を未完了試合の第1セットへ移行。日付・チーム名・点数は推測せず未設定とする。
4. version 4の保存成功後だけ移行結果を採用する。失敗時に初期化や空データ保存をしない。元のv1/v2/v3キーを削除・上書きしない。
5. v1/v2は新ブロック・新レセプションを拒否し、v3は新レセプションを拒否する。v4保存時にも検証する。
6. 保存直列化、復元前の入力抑止、失敗表示・再試行を維持。旧記録と新記録のUndoは元の操作順で処理する。

### レセプションA/B/ミス（Issue #133、元仕様 #131）

- 入力位置はブロックとサーブの間。左からA（緑）、B（青）、ミス（赤）。1タップ1イベントで新本数と内訳を加算し、得点は自動変更しない。
- Aはセッターがほとんど動かずトス可能な返球。Bは移動してトス可能、またはセッター以外がトスする返球を含む。ミスは従来基準。判定は記録者に委ねる。Cボタンや毎回の確認は設けない。
- 永続イベントはreceptionA / receptionB / receptionMiss。旧success / missと区別し、旧成功を推測分類しない。旧ミスも新本数に混ぜない。
- 新本数totalはA+B+新ミス。旧本数legacyTotalと旧success/missは別集計。入力・セット別・試合合計・過去詳細は共通StatsPanelを使用し、全試合の率は件数合算から計算。
- 旧記録があるときだけ旧本数・成功・ミス・旧成功率と「新方式のみ」を表示。率は小数1桁、分母0は0.0%。
- 項目名20px、ボタン文字18px、最小高さ56pxを維持。ブロックと同じ3等分。補助行を折り返し、ボタンを縮小しない。
- 全14入力の600ms強調と既存Undo解除、直前入力ラベルを維持。新ミスと旧ミスもラベルで区別。
- ローカルWebの表示変更は利用者確認済み。タップ・Undo・再読み込み復元、Android/Safari実機は未確認。自動保存検証は独立したテスト用メモリストレージで行う。

### ブロック3結果（Issue #124）

- 入力カード順はスパイク、ディグ、ブロック、レセプション、サーブ（Issue #125で#1仕様へ統合）。結果・履歴の順はスパイク、ディグ、レセプション、サーブ、ブロックのまま維持する。
- 左から「飛んだ」（未接触・茶）、「ワンタッチ」（接触したが得点なし・青）、「成功」（ブロック得点・従来の緑）。1タップ1イベントで排他的に記録し、スコアは手動のまま。
- 新イベントのoutcomeは `jumped` / `touch` / `blockPoint`。総ジャンプ数は3内訳の合計。新成功はワンタッチに重複加算しない。
- 旧 `success` / `failure` は元のイベントとして保持し、旧本数・成功・失敗・従来の成功率を「旧記録」に表示する。旧失敗を新分類へ推測変換しない。旧本数は総ジャンプ数に含めない。
- 新総数は `total`、旧本数は `legacyTotal` としてイベントから導出する。集計値を保存しない。
- 旧記録を含む試合へ新結果を追記できる。旧記録がある場合だけ旧記録欄と「新方式のみ」注記を表示する。
- 入力・セット別・試合合計・過去詳細は共通StatsPanelで総数、3内訳、2率を表示。率は件数合算から小数1桁、分母0は0.0%。
- 項目名20px、ボタン最小高さ56px・文字18pxを維持し、3等分の幅を使う。ブロックの集計は補助行に折り返す。
- 一時強調は全スタッツ入力で受付成功後600ms。最後の1ボタンに白い内枠を重ね、基本色・寸法・文字・位置を変えない。onPressのみで記録し、連打は全件受け付け、最後の受付からタイマーを更新する。拒否・例外・無効入力では新しい強調を出さない。古いタイマーは世代番号で無効化する。
- Undo、試合・セット切替では表示前に強調を解除する。画面離脱時はタイマーを破棄し、アンマウント後に更新しない。強調は保存しない。点数ボタンには適用しない。確認ダイアログや追加入力はない。
- Undo欄の接頭辞は「直前の入力：」。操作内容と表示条件は維持する。
- 直前入力ラベルは旧ブロックに「旧記録」を付け、新旧成功を区別する。点数との操作順Undoとセット境界は既存方式を維持する。

### データが失われる条件

- OS設定からアプリデータを消去、またはアプリをアンインストールした場合
- Webでサイトストレージを削除、または別オリジン（URL・ポートを含む）で開いた場合
- 未保存表示中にアプリを強制終了し、メモリ上に未保存操作があった場合
- 将来の保存形式変更で移行処理を実装しなかった場合

AsyncStorageは暗号化ストレージではないため、秘密情報・認証情報は保存しない。

## 未確定・未実装

- 過去記録消失の報告：原因不明・利用者への聞き取りと調査待ち。Issue #124で修正済みとはしない。
- Issue #124のモバイル幅プレビュー、Android / Safari実機での表示・タップ・スクロール・復元確認

- スパイク3分類の判定基準、各率の正式定義
- 日別集計、選手別記録、設定
- 正式ルールに基づくセット／試合自動終了
- スタッツと得失点の自動連動
- クラウド同期、ログイン、課金
- 今回の変更を反映したAPK作成、配布、Web公開版更新
- Safari実機確認

## 実機確認

- 2026-08-31：Android Expo Goで通常モード入力画面とUndoを確認済み（依頼主確認）
- 2026-08-31：Android Expo Goでversion 1の端末内保存、再起動復元、復元後Undo、Undo後再保存を確認済み（依頼主確認）
- version 2の試合・セット・履歴機能はAndroid実機未確認

## APK配布前確認

- 2026-08-31：自動テスト26件、TypeScript、ESLint、Expo Doctor 18項目、Expo依存関係検査に合格
- 2026-08-31：Android向けHermes本番バンドル生成に成功（開発サーバー不要のJSバンドル成立を確認）
- 入力画面の「セット終了」はスタッツと同じ縦ScrollViewの末尾にあり、固定Undo欄の外側でスクロールして表示・操作できる構造
- 固定Undo欄はScrollViewの兄弟要素で、セット終了ボタン自体を覆う固定配置・絶対配置ではない
- npm auditは推移的依存にhigh 9件、moderate 10件を報告。主にMetro／Expo CLIのビルド処理、画像解析、PostCSS、iOS設定用依存に関するもの
- auditの自動修正はExpo 57への破壊的更新を要求するため実施しない。管理外の画像・CSS・source mapをビルド入力へ追加しない
- Android単独APKでのオフライン起動、version 2保存・復元、2セット試合、履歴、記録再開は未確認
- Expo Goと単独APKはAndroid上の保存領域が別であり、Expo Go内の記録はAPKへ自動移行されない

## 主な構造

- `app/index.tsx`：画面遷移と各画面
- `components/stats-panel.tsx`：スタッツ表示・入力
- `components/score-controls.tsx`：現在セットの手動点数
- `components/result-list.tsx`：セット結果の縦一覧
- `lib/volleyball-stats.ts`：スタッツ集計・率
- `lib/match-domain.ts`：試合・セット・点数・Undo・結果・合計ロジック
- `lib/match-storage.ts`：version 4検証、直列保存、version 1 / 2 / 3移行
- `hooks/use-match-database.ts`：復元、自動保存、失敗表示用状態
- `tests/`：スタッツ、試合、保存、移行の自動テスト

## Issue #125の検証メモ

- 作業元：main / f7e3c39005868dae1feddcd644d154e4c7545ce4。#124は未コミット差分として存在し、そのまま保持して統合。
- 開始時は5テストファイル38件。過去の600ms一時強調テストは現行対象に存在しないため、既存Nodeテスト環境でタイマーと受付・破棄の検証を追加。
- 旧v1/v2から移行、新3結果と点数を追記、別Repositoryで復元、Undo、再保存、再復元の一連の検証を追加。実データは使用しない。
- 過去の31件／17件の報告に対応する実行条件は不明。ローカル履歴から変更消失とは断定しない。

### 未実施の実機確認手順

実データとは別のテスト用アプリ保存領域またはブラウザプロファイルで実施する。

1. 新規テスト試合を開始し、入力順がスパイク・ディグ・ブロック・レセプション・サーブで、Undo欄が「直前の入力：」であることを確認。
2. モバイル幅でスクロールだけ行い、本数が増えないこと、ブロックの3ボタンと内訳・率が重ならないことを確認。
3. 飛んだ2、ワンタッチ1、成功1を入力し総4・両率25.0%、得点不変を確認。連打が全件記録され、最後のボタンだけ約600ms白枠になることを確認。
4. 成功をUndoし、総3・成功率0.0%・ワンタッチ率33.3%と強調解除を確認。点数入力とUndoの順序も確認。
5. 強調中にセット終了確認へ移動して戻り、強調が残らないことを確認。次セットでも同様。
6. 保存済み表示を待って再読込し、記録を再開、Undo、保存後再読込して状態を確認。テスト用旧v1/v2記録でも旧欄・新方式のみの率と保存復元を確認。
7. 終了後のセット別と試合全体・履歴の件数と率を確認。AndroidとSafariはそれぞれ別途実施する。
