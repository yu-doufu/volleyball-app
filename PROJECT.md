# バレーボール試合記録アプリ

## 今回の実装

- 通常モードのチーム全体入力画面（1画面）
- サーブ、レセプション、ブロック、ディグ、スパイクの合計12入力
- 各項目の本数、全内訳、暫定率の表示
- 入力履歴に基づく「1つ戻す」と直前入力の表示
- 入力イベント履歴の端末内自動保存と起動時復元
- 保存中、保存済み、未保存、復元失敗の画面表示と再試行
- Android、iOS、Webで共有するReact Native画面と純粋な集計ロジック
- 集計値と率は保存せず、復元したイベント履歴から再計算する

## 端末内保存

- AsyncStorageを使用し、通信なしでAndroid、iOS、Webの各端末・ブラウザ内へ保存する
- 保存キー：`volleyball-app.active-session.v1`
- 保存形式：version 1。`activeSession` にイベント履歴と更新日時を保持する
- 書込は直列キューで実行し、連続入力やUndoで古い書込が新しい記録を上書きしない
- 復元完了前は入力を無効化し、空データで既存記録を上書きしない
- 読込失敗、不正データ、未対応versionは自動削除・上書きせず、再試行可能なエラーを表示する
- 書込失敗時も最新記録をメモリに保持し、次の入力または「再試行」で再保存する

### データが失われる条件

- Expo Goまたは単独アプリのアプリデータをOS設定から消去した場合
- アプリをアンインストールした場合（OSやバックアップ設定による例外を除く）
- Webで対象サイトのストレージを削除した場合、または別オリジン（URL・ポートを含む）で開いた場合
- 保存失敗表示中にアプリを強制終了し、メモリ上の未保存入力が残っていた場合
- 将来、保存キーや形式を移行する際に適切な移行処理を実装しなかった場合

AsyncStorageは暗号化ストレージではないため、秘密情報や認証情報は保存しない。

## 入力分類

- サーブ：成功、エース、ミス（成功にエースは含めない）
- レセプション：成功、ミス
- ブロック：成功、失敗
- ディグ：成功、ミス
- スパイク：成功、通常、ミス

スパイク3分類は監督要望だが、各分類の厳密な判定基準は確認待ち。現時点では分類名をそのまま記録するだけで、得失点やスコアには連動させない。

## 暫定の率計算

以下は監督確認前の暫定式。カウントは整数で保持し、表示時だけ小数1桁へ丸める。分母が0なら0.0%とする。

- サーブ決定率 = エース数 ÷ サーブ本数 × 100
- サーブミス率 = ミス数 ÷ サーブ本数 × 100
- レセプション成功率 = 成功数 ÷ 本数 × 100
- ブロック成功率 = 成功数 ÷ 本数 × 100
- ディグ成功率 = 成功数 ÷ 本数 × 100
- スパイク決定率 = 成功数 ÷ 本数 × 100

## 未確定事項

- スパイクの「成功」「通常」「ミス」の厳密な判定基準
- 各率の正式な定義
- 入力が得失点やスコアへ与える影響
- 実際の試合中に必要な文字サイズ、色、スクロール量の最終調整

## 今回は未実装

- 保存、試合開始・終了、スコア連動、履歴
- 選手選択、設定
- ログイン、課金
- APK作成、配布、外部公開
- Safariを含むWeb実機確認
- 複数試合の保存・履歴、試合開始・終了、対戦相手入力
- スコア連動、クラウド同期、ログイン

## 実機確認

- 2026-08-31：Android Expo Goで通常モード入力画面と「1つ戻す」を確認済み（依頼主確認）
- 2026-08-31：Android Expo Goで端末内保存、再起動後の復元、復元後のUndo、Undo後の再保存を確認済み（依頼主確認）

## 構造

- `app/index.tsx`：表示とボタン操作
- `lib/volleyball-stats.ts`：入力イベント、集計、率計算、Undo
- `lib/session-storage.ts`：保存形式の検証、読込、順序保証付き書込
- `hooks/use-persistent-match.ts`：復元・自動保存と画面向け状態
- `tests/volleyball-stats.test.ts`：全12入力と集計ルールの自動テスト
- `tests/session-storage.test.ts`：保存・復元、順序、失敗、不正データの自動テスト
