# バレーボール試合記録アプリ

## 今回の実装

- 通常モードのチーム全体入力画面（1画面）
- サーブ、レセプション、ブロック、ディグ、スパイクの合計12入力
- 各項目の本数、全内訳、暫定率の表示
- 入力履歴に基づく「1つ戻す」と直前入力の表示
- Android、iOS、Webで共有するReact Native画面と純粋な集計ロジック
- 入力はメモリ内だけに保持し、アプリの再読み込みで消える

## 入力分類

- サーブ：成功、エース、ミス（成功にエースは含めない）
- レセプション：成功、ミス
- ブロック：成功、失敗
- ディグ：成功、ミス
- スパイク：成功、通常、ミス

スパイク3分類は監督要望だが、各分類の厳密な判定基準は確認待ち。現時点では分類名をそのまま記録するだけで、得失点やスコアには連動させない。

## 暫定の率計算

以下は監督確認前の暫定式。カウントは整数で保持し、表示時だけ小数1桁へ丸める。分母が0なら0.0%とする。

- サーブ決定率 = エース数 ÷ サーブ本数 × 100
- サーブミス率 = ミス数 ÷ サーブ本数 × 100
- レセプション成功率 = 成功数 ÷ 本数 × 100
- ブロック成功率 = 成功数 ÷ 本数 × 100
- ディグ成功率 = 成功数 ÷ 本数 × 100
- スパイク決定率 = 成功数 ÷ 本数 × 100

## 未確定事項

- スパイクの「成功」「通常」「ミス」の厳密な判定基準
- 各率の正式な定義
- 入力が得失点やスコアへ与える影響
- 実際の試合中に必要な文字サイズ、色、スクロール量の最終調整

## 今回は未実装

- 保存、試合開始・終了、スコア連動、履歴
- 選手選択、設定
- ログイン、課金
- APK作成、配布、外部公開
- Safariを含むWeb実機確認

## 実機確認

- 2026-08-31：Android Expo Goで通常モード入力画面と「1つ戻す」を確認済み（依頼主確認）

## 構造

- `app/index.tsx`：表示とボタン操作
- `lib/volleyball-stats.ts`：入力イベント、集計、率計算、Undo
- `tests/volleyball-stats.test.ts`：全12入力と集計ルールの自動テスト
